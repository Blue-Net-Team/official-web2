<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bluenet.web.infrastructure.repository.mapper.VenueMapper">
    <resultMap id="BaseResultMap" type="com.bluenet.web.infrastructure.repository.dataobject.VenueDO">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="subtitle" property="subtitle"/>
        <result column="description" property="description"/>
        <result column="image_file_id" property="imageFileId"/>
        <result column="sort_order" property="sortOrder"/>
    </resultMap>

    <select id="selectAllOrderBySortOrderDesc" resultMap="BaseResultMap">
        SELECT
            v.id,
            v.name,
            v.subtitle,
            v.description,
            v.image_file_id,
            v.sort_order
        FROM tb_venue v
        ORDER BY v.sort_order DESC, v.id ASC
    </select>

    <select id="selectVenueById" resultMap="BaseResultMap">
        SELECT
            v.id,
            v.name,
            v.subtitle,
            v.description,
            v.image_file_id,
            v.sort_order
        FROM tb_venue v
        WHERE v.id = #{id}
    </select>
</mapper>
