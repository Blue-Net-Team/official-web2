<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bluenet.web.infrastructure.repository.mapper.UserExperienceMapper">
    <resultMap id="BaseResultMap" type="com.bluenet.web.infrastructure.repository.dataobject.UserExperienceDO">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="type" property="type"/>
        <result column="title" property="title"/>
        <result column="content" property="content"/>
        <result column="start_time" property="startTime"/>
        <result column="end_time" property="endTime"/>
    </resultMap>

    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT *
        FROM tb_user_experience
        WHERE user_id = #{userId}
        ORDER BY start_time DESC
    </select>

    <select id="selectByUserIdAndType" resultMap="BaseResultMap">
        SELECT *
        FROM tb_user_experience
        WHERE user_id = #{userId}
          AND type = #{type}
        ORDER BY start_time DESC
    </select>

    <select id="countByUserIdAndType" resultType="long">
        SELECT COUNT(*)
        FROM tb_user_experience
        WHERE user_id = #{userId}
          AND type = #{type}
    </select>
</mapper>
