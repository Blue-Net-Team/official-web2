<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bluenet.web.infrastructure.repository.mapper.LearningPathMapper">

    <!-- 根据方向查询学习步骤列表 -->
    <select id="selectByDirection" resultType="com.bluenet.web.infrastructure.repository.dataobject.DirectionLearningStepDO">
        SELECT
            id,
            direction,
            sort_order AS sortOrder,
            title,
            related_url AS relatedUrl
        FROM tb_direction_learning_step
        WHERE direction = #{direction}
        ORDER BY sort_order ASC, id ASC
    </select>

    <!-- 根据ID查询学习步骤 -->
    <select id="selectLearningStepById" resultType="com.bluenet.web.infrastructure.repository.dataobject.DirectionLearningStepDO">
        SELECT
            id,
            direction,
            sort_order AS sortOrder,
            title,
            related_url AS relatedUrl
        FROM tb_direction_learning_step
        WHERE id = #{id}
    </select>

    <!-- 查询某方向当前最大排序值，用于新增步骤追加到末尾 -->
    <select id="selectMaxSortOrder" resultType="java.lang.Integer">
        SELECT MAX(sort_order)
        FROM tb_direction_learning_step
        WHERE direction = #{direction}
    </select>

    <!-- 批量覆盖某方向学习步骤的排序值 -->
    <update id="batchUpdateSortOrder">
        UPDATE tb_direction_learning_step
        SET sort_order = CASE id
        <foreach collection="items" item="item">
            WHEN #{item.id} THEN #{item.sortOrder}
        </foreach>
        END
        WHERE direction = #{direction}
        AND id IN
        <foreach collection="items" item="item" open="(" separator="," close=")">
            #{item.id}
        </foreach>
    </update>

</mapper>
