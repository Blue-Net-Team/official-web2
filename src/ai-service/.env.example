# =============================================================================
# TBD-RAG 环境变量配置
# 复制此文件为 .env 并按需修改
# 此文件不包含真实密钥，可提交到 Git 仓库作为模板
# =============================================================================

# ---------------------------------------------------------------------------
# API 密钥（根据使用的提供商填写）
# 硅基流动: https://cloud.siliconflow.cn/account/ak
# DeepSeek: https://platform.deepseek.com/api_keys
# ---------------------------------------------------------------------------
SILICONFLOW_API_KEY=sk-your-siliconflow-api-key-here
DEEPSEEK_API_KEY=sk-your-deepseek-api-key-here

# ---------------------------------------------------------------------------
# MinIO 安全凭证（docker-compose 使用）
# 注意：必须与正在运行的容器中的 MinIO 密码一致
# ---------------------------------------------------------------------------
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin

# ---------------------------------------------------------------------------
# Milvus 连接配置（本地运行，无需认证）
# ---------------------------------------------------------------------------
TBD_RAG_MILVUS_URI=http://127.0.0.1:19530
TBD_RAG_MILVUS_TOKEN=

# ---------------------------------------------------------------------------
# PostgreSQL / pgvector 连接配置
# ---------------------------------------------------------------------------
TBD_RAG_PGVECTOR_URI=postgresql://postgres:password@127.0.0.1:5433/rag
TBD_RAG_PGVECTOR_POOL_SIZE=10

# ---------------------------------------------------------------------------
# Collection 名称
# ---------------------------------------------------------------------------
TBD_RAG_TAGS_COLLECTION_NAME=tags_collection
TBD_RAG_CHUNKS_COLLECTION_NAME=chunks_collection
TBD_RAG_DOCS_COLLECTION_NAME=docs_collection

# ---------------------------------------------------------------------------
# 向量配置（bge-m3 默认 1024 维）
# ---------------------------------------------------------------------------
TBD_RAG_VECTOR_DIMENSION=1024
TBD_RAG_VECTOR_INDEX_TYPE=HNSW
TBD_RAG_VECTOR_METRIC_TYPE=COSINE

# ---------------------------------------------------------------------------
# 向量存储后端配置
# 可选值: milvus | pgsql
# ---------------------------------------------------------------------------
TBD_RAG_VECTOR_STORE_BACKEND=milvus

# ---------------------------------------------------------------------------
# 语义分片配置
# ---------------------------------------------------------------------------
TBD_RAG_CHUNK_MAX_TOKENS=4000

# ---------------------------------------------------------------------------
# Ollama 本地服务地址（仅当使用 ollama 提供商时生效）
# ---------------------------------------------------------------------------
# TBD_RAG_OLLAMA_BASE_URL=http://localhost:11434

# ---------------------------------------------------------------------------
# Embedding 模型配置
# 可选提供商: siliconflow | ollama
# ---------------------------------------------------------------------------
TBD_RAG_EMBEDDING_PROVIDER=siliconflow
# TBD_RAG_EMBEDDING_MODEL=BAAI/bge-m3

# ---------------------------------------------------------------------------
# LLM 配置
# 可选提供商: siliconflow | deepseek | ollama
# ---------------------------------------------------------------------------
TBD_RAG_LLM_PROVIDER=siliconflow
# TBD_RAG_LLM_MODEL=deepseek-ai/DeepSeek-V3
TBD_RAG_LLM_TEMPERATURE=0.7
TBD_RAG_LLM_TIMEOUT=60

# ---------------------------------------------------------------------------
# Reranker 模型配置
# 可选提供商: siliconflow | ollama
# ---------------------------------------------------------------------------
TBD_RAG_RERANKER_PROVIDER=siliconflow
# TBD_RAG_RERANKER_MODEL=BAAI/bge-reranker-v2-m3
