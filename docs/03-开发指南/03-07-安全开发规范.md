# 安全开发规范

## 密码加密

- **前端 SHA256 + 后端 BCrypt**，后端不接触密码明文。
- 后端涉及密码的逻辑（`User.resetPassword()`、`createUser()` 等）**不得**对密码再做额外哈希，直接 `passwordEncoder.encode(前端传来的哈希值)` 即可。

## 认证与授权

### JWT

- 使用 jjwt 0.12.5 生成和校验 JWT。
- 密钥通过环境变量注入，禁止硬编码。
- 设置合理的过期时间，refresh token 与 access token 分离。

### 接口权限

- 所有 REST 接口必须使用 `@RequiresPermission` 注解，指定 `value`（全局唯一权限标识）、`name`（显示名称）、`access`（访问级别）。
- `value` 在整个系统必须全局唯一，`PermissionScanner` 启动时扫描并校验，重复将导致启动失败。**不允许修改 `PermissionScanner` 逻辑**。
- `AccessLevel`：`PUBLIC`（公开）、`AUTHENTICATED`（需登录）、`PROTECTED`（需特定权限，默认）。

## 文件权限

文件访问权限根据关联的业务实体动态控制：

| 文件类型 | 权限判断逻辑 |
|----------|-------------|
| `work` | `currentUser.id == answer.user_id` OR `currentUser.role >= ROLE_MEMBER` |
| `assessment_attachment` | `currentUser.direction == question.direction` |
| `avatar` | `currentUser.role >= ROLE_MEMBER` 或 `currentUser.avatarId == fileId` |
| `normal_img` / `qrcode` | 公开可见 |

## 密钥与第三方服务

- 涉及 GitHub OAuth、阿里云 OSS、MinIO、SMTP、AI 服务密钥等配置时，必须通过环境变量注入。
- **禁止**在代码、日志、测试、文档中硬编码真实密钥。
- `.env` 文件不得提交到仓库（已加入 `.gitignore`）。
- 生产环境使用专门的 Secrets 管理工具（如 GitHub Secrets、Vault）。

## CORS 与 Cookie

- 生产环境 CORS 只允许配置的域名。
- Cookie 设置 `HttpOnly`、`Secure`、`SameSite` 属性。

## 预签名 URL

- 使用 MinIO 预签名 URL 时，`MINIO_PUBLIC_URL` 必须是浏览器实际访问 MinIO 的地址。
- Docker 环境填宿主机可访问的外部地址。
- 不要设置 `MINIO_SERVER_URL` 环境变量给 MinIO 容器。

## 常见安全故障

### 密码验证失败

**原因**：后端对密码做了额外哈希（如再次 SHA256 或多次 BCrypt）。

**解决**：后端密码逻辑直接 `passwordEncoder.encode(前端传来的SHA256哈希值)`，不得再做额外处理。

### 预签名 URL 上传失败（SignatureDoesNotMatch）

**原因**：`MINIO_PUBLIC_URL` 配置不正确，或请求 Host 与签名时不一致。

**解决**：确保 `MINIO_PUBLIC_URL` 是浏览器实际访问 MinIO 的地址。

## 相关文档

- [03-02-后端开发规范](./03-02-后端开发规范.md)
- [04-01-环境配置](../04-运维部署/04-01-环境配置.md)
- [../SECURITY.md](../SECURITY.md)
