# 前端开发规范

## 技术栈

| 技术 | 版本 | 说明 |
|------|------|------|
| React | 19.2.0 | UI框架 |
| Next.js | 15.1.0 | 全栈框架，支持SSR |
| TypeScript | 5.9.3 | 类型安全 |
| Ant Design | 6.3.0 | UI组件库 |
| Zustand | 5.0.11 | 状态管理 |
| Axios | 1.13.5 | HTTP客户端 |
| pnpm | 9.15.0 | 包管理器 |

## 项目结构

```
src/frontend/
├── public/                    # 静态资源
├── src/
│   ├── apis/                  # API接口层
│   │   ├── schema/            # 类型定义
│   │   │   ├── direction.dto.ts
│   │   │   ├── enumerate.ts   # 枚举定义
│   │   │   └── type.ts
│   │   ├── services/          # API服务
│   │   │   ├── auth.service.ts
│   │   │   ├── college.service.ts
│   │   │   ├── competition.service.ts
│   │   │   ├── direction.service.ts
│   │   │   ├── enroll.service.ts
│   │   │   ├── file.service.ts
│   │   │   ├── member.service.ts
│   │   │   └── user.service.ts
│   │   ├── client.ts          # Axios实例配置
│   │   └── config.ts          # API配置
│   │
│   ├── app/                   # Next.js页面路由
│   │   ├── (public)/          # 公开页面组
│   │   │   ├── (home)/        # 首页
│   │   │   ├── (other)/       # 其他公开页面
│   │   │   │   ├── direction/[slug]/  # 方向详情
│   │   │   │   ├── enroll/           # 报名页
│   │   │   │   ├── login/            # 登录页
│   │   │   │   ├── members/          # 成员列表
│   │   │   │   ├── profile/          # 个人中心
│   │   │   │   ├── achievements/     # 团队成就
│   │   │   │   ├── competitions/     # 竞赛列表
│   │   │   │   ├── lab-environment/  # 实验室环境
│   │   │   │   ├── assessment/       # 考核列表
│   │   │   │   ├── forgot-password/  # 忘记密码
│   │   │   │   └── change-password/  # 修改密码
│   │   │   └── layout.tsx
│   │   ├── admin/             # 管理后台
│   │   │   ├── panel/         # 监控仪表盘
│   │   │   ├── enroll/        # 报名审核
│   │   │   ├── users/         # 用户管理
│   │   │   ├── college/       # 学院管理
│   │   │   ├── achievement/   # 成就管理
│   │   │   ├── competition/   # 竞赛管理
│   │   │   ├── venue/         # 场地管理
│   │   │   ├── equipment/     # 设备管理
│   │   │   ├── assessment/    # 考核管理
│   │   │   │   ├── time/      # 考核时间管理
│   │   │   │   ├── question/  # 考题管理
│   │   │   │   └── judge/     # 评分与决策
│   │   │   │       ├── score/   # 评分工作台
│   │   │   │       └── decision/# 录用决策
│   │   │   ├── bug-report/    # Bug 报告管理
│   │   │   ├── qrcode/        # 二维码管理
│   │   │   ├── message-template/ # 消息模板管理
│   │   │   └── permissions/   # 权限管理
│   │   │       ├── role/      # 角色权限分配
│   │   │       └── permission/# 权限角色分配
│   │   ├── globals.css        # 全局样式
│   │   └── layout.tsx         # 根布局
│   │
│   ├── assets/                # 静态图片资源
│   │   ├── HomeDirectionIntroduce/
│   │   ├── HomeRecruitmentProcess/
│   │   ├── Login/
│   │   ├── direction/         # 方向介绍图片
│   │   └── icon/              # 图标
│   │
│   ├── components/            # 公共组件
│   │   ├── Admin/             # 管理后台组件
│   │   ├── Direction/         # 方向详情组件
│   │   ├── Footer/            # 页脚
│   │   ├── Home/              # 首页组件
│   │   ├── MemberProfile/     # 成员主页组件
│   │   ├── Members/           # 成员列表组件
│   │   ├── Profile/           # 个人中心组件
│   │   ├── PublicNavbar/      # 公开导航栏
│   │   ├── AuthProvider.tsx   # 认证提供者
│   │   ├── ThemeProvider.tsx  # 主题提供者
│   │   └── Wrapper.tsx        # 包装器
│   │
│   ├── mocks/                 # Mock数据
│   ├── stores/                # Zustand状态管理
│   │   └── authStore.ts       # 认证状态
│   │
│   ├── styles/                # 样式文件
│   ├── types/                 # 类型定义
│   └── utils/                 # 工具函数
│
├── eslint.config.mjs
├── next.config.ts
├── package.json
├── pnpm-lock.yaml
└── tsconfig.json
```

## 页面清单

### 1. 首页

**路径**: `/`

**功能说明**:
- 展示实验室介绍、团队合照
- 展示各方向介绍（计算机视觉、结构设计、嵌入式开发）
- 展示竞赛、专利、论文等成就
- 引导用户点击报名按钮

**组件结构**:
| 组件 | 说明 |
|------|------|
| TopContent | 顶部Banner区域 |
| DirectionIntroduce | 三个方向介绍卡片 |
| RecruitmentProcess | 招新流程展示 |
| Competitions | 竞赛展示 |
| FeaturedEquipment | 设备展示 |
| TeamVibe | 团队氛围展示 |
| AchievementAndResources | 成就与资源 |

---

### 2. 方向详情页

**路径**: `/direction/[slug]`

**路由参数**: `slug` = `cv` | `embed` | `struct`

**功能说明**:
- 展示方向详细信息
- 学习路径展示
- 技术栈介绍
- 就业方向

**组件结构**:
| 组件 | 说明 |
|------|------|
| HeroSection | 方向头部展示 |
| LearningPath | 学习路径 |
| TechStack | 技术栈 |
| CareerSection | 就业方向 |
| RecruitmentInfo | 招新信息 |

---

### 3. 报名页

**路径**: `/enroll`

**功能说明**:
- 用户填写报名信息
- 支持同一学号更新报名信息
- 头像上传功能

**表单字段**:
| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 姓名 | 输入框 | 是 | 真实姓名 |
| 学号 | 输入框 | 是 | 12-13位数字 |
| 邮箱 | 输入框 | 是 | 用于接收通知 |
| 学院 | 下拉选择 | 是 | 关联学院表 |
| 专业 | 输入框 | 是 | - |
| 年级 | 下拉选择 | 是 | 大一/大二 |
| 方向 | 单选 | 是 | 计算机视觉/结构设计/嵌入式开发 |
| 内推码 | 输入框 | 否 | 8位随机码 |
| 头像 | 上传组件 | 是 | 支持图片上传预览 |

---

### 4. 登录页

**路径**: `/login`

**功能说明**:
- 支持学号登录和邮箱登录切换
- 只能通过报名后发放账号登录，无注册入口

**登录方式**:
- 学号 + 密码登录
- 邮箱验证码登录

---

### 5. 成员列表页

**路径**: `/members`

**功能说明**:
- 按方向分类展示成员
- 方向筛选标签

**组件结构**:
| 组件 | 说明 |
|------|------|
| Members | 成员列表容器 |
| MemberCard | 成员卡片 |

---

### 6. 成员详情页

**路径**: `/members/[id]`

**功能说明**:
- 展示成员详细信息
- 所有人可查看

**组件结构**:
| 组件 | 说明 |
|------|------|
| MemberProfileContent | 主内容区 |
| MemberProfileSidebar | 侧边栏 |
| ExperiencePanel | 经历面板 |

---

### 7. 个人中心

**路径**: `/profile`

**功能说明**:
- 查看和编辑个人信息
- 查看我的考核

**组件结构**:
| 组件 | 说明 |
|------|------|
| ProfileSidebar | 侧边栏 |
| ProfileTabs | 标签页切换 |
| ProfileInfo | 个人信息设置 |
| AssessmentList | 考核列表 |
| ExperienceSection | 经历管理 |

---

### 8. 团队成就页

**路径**: `/achievements`

**功能说明**:
- 展示团队在学术研究、技术创新和学科竞赛中取得的荣誉与成果
- 支持按类型（论文/专利/竞赛）、奖项级别（国家级/省级/校级）、年份筛选
- 瀑布流布局展示成就卡片
- 顶部展示统计数据（总数、国家级、省级、校级）

**组件结构**:
| 组件 | 说明 |
|------|------|
| AchievementStats | 成就统计卡片 |
| AchievementFilter | 筛选条件栏 |
| AchievementCard | 单个成就卡片 |

---

### 9. 竞赛列表页

**路径**: `/competitions`

**功能说明**:
- 展示团队参加的各类竞赛信息
- 分页展示竞赛卡片

**组件结构**:
| 组件 | 说明 |
|------|------|
| CompetitionCard | 竞赛卡片 |
| BackgroundDecorations | 背景装饰 |

---

### 10. 实验室环境页

**路径**: `/lab-environment`

**功能说明**:
- 展示实验室场地信息与设备介绍
- 从后端获取场地和设备数据（Server Component，ISR 3600s）

**组件结构**:
| 组件 | 说明 |
|------|------|
| VenueCard | 场地卡片 |
| EquipmentCard | 设备卡片 |
| BackgroundDecorations | 背景装饰 |

---

### 11. 考核列表页

**路径**: `/assessment`

**功能说明**:
- 展示当前可参加的考核时间安排
- 按方向筛选，显示考核时间、状态（未开始/进行中/已结束）
- 支持组队考核（创建队伍/加入队伍）

**组件结构**:
| 组件 | 说明 |
|------|------|
| AssessmentCard | 考核时间卡片 |

---

### 12. 考核答题页

**路径**: `/assessment/[timeId]/questions`

**功能说明**:
- 展示指定考核时间下的考题目录
- 显示题目总数、已作答数、未答数、总分统计
- 支持组队功能（创建/加入队伍）
- 点击题目进入答题详情

---

### 13. 题目详情页（答题界面）

**路径**: `/assessment/[timeId]/questions/[questionId]`

**功能说明**:
- 单题答题界面
- 支持多种题型：单选题、多选题、文件上传题、算法题
- 算法题支持在线代码编辑与提交（Beta）

**组件结构**:
| 组件 | 说明 |
|------|------|
| QuestionDetailPage | 题目详情与答题主组件 |

---

### 14. 忘记密码页

**路径**: `/forgot-password`

**功能说明**:
- 四步流程重置密码：验证学号 → 验证邮箱 → 输入验证码 → 设置新密码
- 验证码通过邮件发送

---

### 15. 修改密码页

**路径**: `/change-password`

**功能说明**:
- 已登录用户修改密码
- 两步流程：验证原密码 → 设置新密码
- 修改成功后自动退出登录

---

### 16. 管理后台

**路径**: `/admin`

**功能说明**:
- 管理员操作入口，默认重定向到 `/admin/panel`
- 包含报名审核、考核管理、用户管理、权限管理等功能

**组件结构**:
| 组件 | 说明 |
|------|------|
| AdminHeadBar | 顶部导航 |
| AdminSideBar | 侧边菜单 |

#### 管理后台子页面

| 页面 | 路径 | 功能说明 |
|------|------|----------|
| 监控仪表盘 | `/admin/panel` | API 请求量趋势、接口访问排名、响应时间排名 |
| 报名审核 | `/admin/enroll` | 报名列表审核（通过/拒绝）、统计数据 |
| 用户管理 | `/admin/users` | 用户 CRUD、批量操作、重置密码、详情查看 |
| 学院管理 | `/admin/college` | 学院列表增删改查 |
| 成就管理 | `/admin/achievement` | 成就数据增删改查、筛选 |
| 竞赛管理 | `/admin/competition` | 竞赛数据管理 |
| 场地管理 | `/admin/venue` | 实验室场地管理 |
| 设备管理 | `/admin/equipment` | 实验室设备管理 |
| 考核时间管理 | `/admin/assessment/time` | 考核时间增删改查、方向/年级筛选 |
| 考题管理 | `/admin/assessment/question` | 考题增删改查、支持单选/多选/文件上传/算法题 |
| 评分与决策 | `/admin/assessment/judge/score` | 题目视图评分、人员视图评分、录用决策、批量下载 |
| 录用决策 | `/admin/assessment/judge/decision` | 候选人通过/淘汰决策、发布结果通知 |
| Bug 报告管理 | `/admin/bug-report` | Bug 报告列表、状态更新、GitHub Issue 关联 |
| 二维码管理 | `/admin/qrcode` | 咨询群/考核群二维码管理 |
| 消息模板管理 | `/admin/message-template` | 邮件消息模板编辑 |
| 权限角色管理 | `/admin/permissions/role` | 角色权限分配（SUPER_ADMIN/DIRECTION_ADMIN/MEMBER/CANDIDATE） |
| 权限管理 | `/admin/permissions/permission` | 按权限分配角色 |
| 内推海报模板设计器 | `/admin/referral-poster-design` | 内推海报模板的可视化调参工具（见下方说明） |

#### 内推海报模板设计器

**路径**: `/admin/referral-poster-design`

**用途**: 这是一个**开发/运营辅助工具**，不属于业务功能，不会出现在管理后台导航菜单中，只能通过直接访问 URL 进入。

**背景**: 个人主页"我要内推"功能的海报由"静态底图 + 动态元素（内推码、二维码、内推人姓名）"在 Canvas 上叠加合成，动态元素的位置/字号/颜色定义在 `src/components/Profile/ReferralPoster/poster-templates.ts` 中。

**使用场景**:
- 新增海报模板时，需要确定动态元素在新底图上的精确坐标
- 调整现有模板的文字大小、颜色、二维码尺寸

**使用方式**:
1. 页面左侧加载当前模板底图，三个动态元素（内推码、二维码、内推人）以半透明标注块显示在初始位置
2. 用鼠标拖拽标注块到目标位置，或在右侧表单中直接输入 X/Y/字号/尺寸/颜色
3. 点击"导出配置"，将 JSON 粘贴到 `poster-templates.ts` 的 `POSTER_TEMPLATES` 数组中（新增模板需同时把新底图放入 `public/referral-posters/`）

**注意**: 该页面读取的是 `poster-templates.ts` 中第一个模板作为初始值；修改配置后需回到个人主页"我要内推"弹窗中确认最终合成效果。

---

## 枚举值定义

### 枚举使用规范

#### 枚举格式对照

前端与后端枚举格式保持一致，API通信统一使用**小写下划线**格式：

| 场景 | 格式 | 示例 |
|------|------|------|
| TypeScript类型定义 | 大写下划线 | `'COMPUTER_VISION'` |
| API请求参数 | 小写下划线 | `direction=computer_vision` |
| API响应JSON | 小写下划线 | `"direction": "computer_vision"` |
| URL路径参数（slug） | 小写简写 | `/direction/cv` |

#### 前端枚举定义示例

```typescript
// TypeScript类型定义（大写下划线）
export type Direction = 'COMPUTER_VISION' | 'STRUCTURAL_DESIGN' | 'EMBEDDED';

// 枚举标签映射
export const DIRECTION_LABELS: Record<Direction, string> = {
  COMPUTER_VISION: '计算机视觉',
  STRUCTURAL_DESIGN: '结构设计',
  EMBEDDED: '嵌入式开发',
};

// slug映射（用于URL）
export const DIRECTION_SLUGS: Record<Direction, string> = {
  COMPUTER_VISION: 'cv',
  STRUCTURAL_DESIGN: 'struct',
  EMBEDDED: 'embed',
};
```

#### API请求示例

```typescript
// 正确：使用小写下划线格式
const response = await client.get('/members', {
  params: { direction: 'computer_vision' }
});

// 错误：不要使用大写格式
const response = await client.get('/members', {
  params: { direction: 'COMPUTER_VISION' }  // ❌ 错误
});
```

### Direction（方向）

| 值 | 说明 | slug |
|------|------|------|
| computer_vision | 计算机视觉 | cv |
| structural_design | 结构设计 | struct |
| embedded | 嵌入式开发 | embed |

### EnrollStatus（报名状态）

| 值 | 说明 |
|------|------|
| pending | 待审核 |
| approved | 已通过 |
| rejected | 已拒绝 |

### Gender（性别）

| 值 | 说明 |
|------|------|
| male | 男 |
| female | 女 |
| unknown | 未知 |

### QuestionType（题目类型）

| 值 | 说明 |
|------|------|
| single_choice | 单选题 |
| multiple_choice | 多选题 |
| file_upload | 文件上传 |
| algorithm | 算法题 |

### ProgrammingLanguage（编程语言）

| 值 | 说明 |
|------|------|
| python | Python |
| c | C |
| cpp | C++ |
| java | Java |
| javascript | JavaScript |

### ExperienceType（经历类型）

| 值 | 说明 |
|------|------|
| competition | 竞赛 |
| project | 项目 |
| internship | 实习 |

### AchievementType（成就类型）

| 值 | 说明 |
|------|------|
| paper | 论文 |
| patent | 专利 |
| competition | 竞赛 |

### FileType（文件类型）

| 值 | 说明 |
|------|------|
| avatar | 头像 |
| normal_img | 普通图片 |
| assessment_attachment | 考题附件 |
| work | 考生作品 |
| qrcode | 二维码 |

---

## 权限矩阵

| 页面/功能 | 未登录 | 考生 | 团队成员 | 方向管理员 | 超级管理员 |
|------|--------|------|----------|------------|------------|
| 首页 | ✅ | ✅ | ✅ | ✅ | ✅ |
| 方向详情 | ✅ | ✅ | ✅ | ✅ | ✅ |
| 报名页 | ✅ | - | - | - | - |
| 登录页 | ✅ | - | - | - | - |
| 成员列表 | ✅ | ✅ | ✅ | ✅ | ✅ |
| 成员详情 | ✅ | ✅ | ✅ | ✅ | ✅ |
| 个人中心 | - | ✅ | ✅ | ✅ | ✅ |
| 团队成就 | ✅ | ✅ | ✅ | ✅ | ✅ |
| 竞赛列表 | ✅ | ✅ | ✅ | ✅ | ✅ |
| 实验室环境 | ✅ | ✅ | ✅ | ✅ | ✅ |
| 考核列表 | - | ✅ | ✅ | ✅ | ✅ |
| 题目详情（答题） | - | ✅ | ✅ | ✅ | ✅ |
| 忘记密码 | ✅ | - | - | - | - |
| 修改密码 | - | ✅ | ✅ | ✅ | ✅ |
| 管理后台 | - | - | ✅ | ✅ | ✅ |

**说明**:
- ✅ 表示有权限访问
- - 表示无权限访问

---

## 开发规范

### 命名规范

- **组件文件**: PascalCase（如 `MemberCard.tsx`）
- **样式文件**: camelCase 或 module.css（如 `styles.module.css`）
- **工具函数**: camelCase（如 `RoleUtils.ts`）
- **API服务**: camelCase + .service.ts（如 `auth.service.ts`）

### Server Component 与 ISR 规范

项目使用 Next.js App Router，页面默认为 Server Component。**所有从后端获取数据的 Server Component 页面必须配置 ISR（增量静态再生成）**，否则 build 后内容固定，后端数据变更无法反映到页面上。

#### ISR 配置要求

1. 每个有后端数据获取的页面必须设置 `export const revalidate`
2. `revalidate` **必须是静态数字字面量**，Next.js 不支持变量引用或表达式
3. 默认值为 `3600`（1 小时），可根据业务需求调整
4. 纯静态页面（无后端数据）和 `'use client'` 页面不需要配置

```typescript
// ✅ 正确：静态数字字面量
export const revalidate = 3600

// ❌ 错误：Next.js 不支持变量引用
import { ISR } from '@/config/isr'
export const revalidate = ISR.home  // 编译报错：Unsupported node type "MemberExpression"

// ❌ 错误：运行时表达式
export const revalidate = Number(process.env.REVALIDATE) || 3600  // 编译报错
```

#### 已配置 ISR 的页面

| 页面 | 路径 | revalidate | 说明 |
|------|------|-----------|------|
| 首页 | `(home)/page.tsx` | 3600 | 竞赛列表数据 |
| 竞赛列表 | `competitions/page.tsx` | 3600 | 竞赛数据 |
| 实验室环境 | `lab-environment/page.tsx` | 3600 | 场地 + 设备数据 |
| 方向详情 | `direction/[slug]/page.tsx` | 3600 | 学习路径数据 |
| 团队成就 | `achievements/page.tsx` | 3600 | 成就统计数据 |

#### ISR 配置参考文件

`src/frontend/src/config/isr.ts` 记录了各页面的 revalidate 值作为文档参考。由于 Next.js 限制，实际值必须以字面量写在各页面文件中。

#### 新建页面检查清单

- 页面是否为 Server Component（没有 `'use client'`）？
- 页面是否从后端获取数据（有 `async` 函数调用 API）？
- 如果都是，**必须**添加 `export const revalidate = <秒数>`

### 组件开发

1. 使用函数组件和Hooks
2. 样式优先使用CSS Modules
3. 组件拆分遵循单一职责原则
4. 类型定义放在 `types/` 或 `apis/schema/` 目录

### API调用

1. 所有API调用通过 `apis/services/` 下的服务模块
2. 使用统一的 Axios 实例（`apis/client.ts`）
3. 错误处理统一在拦截器中处理

### 状态管理

1. 全局状态使用 Zustand
2. 认证状态在 `stores/authStore.ts`
3. 组件内部状态使用 React useState

---

## 启动开发

```bash
# 安装依赖
pnpm install

# 启动开发服务器
pnpm dev

# 构建生产版本
pnpm build

# 启动生产服务器
pnpm start

# 代码检查
pnpm lint
```

---

## 文件上传对接

### 概述

改造后的文件上传采用**预签名 URL 直传 OSS** 模式，前端不再将文件二进制流发送到后端，而是分三步完成：

1. **prepare**：向后端申请上传凭证（预签名 URL + callbackToken）
2. **upload**：使用预签名 URL 直接将文件 PUT 到 OSS（MinIO / 阿里云 OSS）
3. **confirm**：上传成功后，携带 callbackToken 通知后端确认

### 三步上传流程

#### Step 1: Prepare（获取预签名上传凭证）

```typescript
// POST /api/v1/file/prepare-upload
// Content-Type: application/json

interface PrepareUploadRequest {
  type: 'AVATAR' | 'NORMAL_IMG' | 'ASSESSMENT_ATTACHMENT' | 'WORK' | 'QRCODE';
  contentType: string;      // 如 'image/jpeg'
  size: number;             // 文件大小（字节）
  md5: string;              // 文件 MD5（Base64 编码）
}

interface PrepareUploadResponse {
  fileId: number;
  uploadUrl: string;        // 直传 OSS 的预签名 PUT URL
  callbackToken: string;    // 后续 confirm 用凭证
  filename: string;
  type: FileType;
}
```

**调用示例**：

```typescript
async function prepareUpload(
  file: File,
  type: FileType
): Promise<PrepareUploadResponse> {
  const md5 = await computeMD5Base64(file); // 使用 spark-md5 等库计算

  const res = await fetch('/api/v1/file/prepare-upload', {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      'X-CSRF-Token': csrfToken, // 已登录用户需携带
    },
    credentials: 'include',
    body: JSON.stringify({
      type,
      contentType: file.type,
      size: file.size,
      md5,
    }),
  });

  if (!res.ok) throw new Error('Prepare upload failed');
  return res.json();
}
```

**权限说明**：

- `AVATAR`、`NORMAL_IMG`：允许未登录用户调用（用于报名表头像等场景）
- 其他类型（`WORK`、`ASSESSMENT_ATTACHMENT`、`QRCODE`）：需要登录
- 匿名调用有 IP 级限流（每 5 秒 1 次）

---

#### Step 2: Upload（直传 OSS）

拿到 `uploadUrl` 后，前端直接使用 HTTP PUT 将文件上传到该 URL，**不携带 Cookie**。

```typescript
async function uploadToOSS(
  file: File,
  uploadUrl: string
): Promise<void> {
  const res = await fetch(uploadUrl, {
    method: 'PUT',
    headers: {
      'Content-Type': file.type,
      // 'Content-Length': file.size.toString(), // 浏览器自动设置
    },
    body: file,
    // 注意：不能携带 Cookie 到 OSS 域名
    credentials: 'omit',
  });

  if (!res.ok) throw new Error('OSS upload failed');
}
```

**重要约束**：

- 必须使用 `PUT` 方法，不能使用 `POST`
- `Content-Type` 必须与 prepare 时传入的一致，否则 OSS 拒绝
- 上传域名是 OSS 域名（如 `http://localhost:9000` 或阿里云域名），不是后端 API 域名
- 预签名 URL 有效期 15 分钟，超时需重新 prepare

---

#### Step 3: Confirm（通知后端确认）

OSS 上传成功后，调用 confirm 接口通知后端校验文件并完成入库。

```typescript
// POST /api/v1/file/confirm-upload
// Content-Type: application/json

interface ConfirmUploadRequest {
  fileId: number;
  callbackToken: string;
}

interface ConfirmUploadResponse {
  fileId: number;
  filename: string;
  type: FileType;
  status: 'ACTIVE' | 'REJECTED';
}
```

**调用示例**：

```typescript
async function confirmUpload(
  fileId: number,
  callbackToken: string
): Promise<ConfirmUploadResponse> {
  const res = await fetch('/api/v1/file/confirm-upload', {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      'X-CSRF-Token': csrfToken,
    },
    credentials: 'include',
    body: JSON.stringify({ fileId, callbackToken }),
  });

  if (!res.ok) throw new Error('Confirm upload failed');
  return res.json();
}
```

**确认结果**：

- `status === 'ACTIVE'`：上传成功，文件可用
- `status === 'REJECTED'`：校验失败（MD5 不匹配或魔数异常），后端已自动清理 OSS 对象，前端应提示用户重新上传

---

### 完整封装示例

```typescript
export async function uploadFile(
  file: File,
  type: FileType
): Promise<{ fileId: number; filename: string }> {
  // 1. Prepare
  const prepared = await prepareUpload(file, type);

  // 2. Upload to OSS
  await uploadToOSS(file, prepared.uploadUrl);

  // 3. Confirm
  const confirmed = await confirmUpload(prepared.fileId, prepared.callbackToken);

  if (confirmed.status === 'REJECTED') {
    throw new Error('文件校验失败，请重新上传');
  }

  return { fileId: confirmed.fileId, filename: confirmed.filename };
}
```

---

### 下载接口变化

#### 单文件下载

`GET /api/v1/file/download/{fileId}` 现在返回 **302 Redirect**，前端需跟随重定向：

```typescript
// 浏览器会自动跟随 302，直接设置 href 即可
window.location.href = `/api/v1/file/download/${fileId}`;

// 或使用 fetch（需处理重定向）
const res = await fetch(`/api/v1/file/download/${fileId}`, {
  credentials: 'include',
  redirect: 'follow',
});
```

#### 批量下载

`GET /api/v1/file/download/batch?fileIds=1,2,3` 返回流式 ZIP，浏览器自动触发下载：

```typescript
window.location.href = `/api/v1/file/download/batch?fileIds=${fileIds.join(',')}`;
```

---

### 注意事项

1. **MD5 计算**：prepare 前必须计算完整文件的 MD5（Base64 编码），后端用于校验文件完整性。
2. **进度监控**：直传 OSS 时可用 `XMLHttpRequest` 替代 `fetch` 以获取上传进度。
3. **断网恢复**：若 Step 2 上传失败（网络中断），可直接重试同一个 `uploadUrl`（在有效期内）；若 URL 过期，需重新执行 Step 1。
4. **旧接口兼容**：`POST /api/v1/file/upload` 仍可用但已标记 `@Deprecated`，建议新功能统一迁移到三步流程。

---

## 相关文档

- [接口设计规范](./03-04-接口设计规范.md)
- [测试规范手册](./03-08-测试规范手册.md)
- [算法判题指南](../05-参考手册/05-03-算法判题指南.md)
