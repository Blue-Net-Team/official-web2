## 1. 审核通过凭据发放

- [x] 1.1 调整审核通过“新建用户”分支，生成 8 位随机初始密码。
- [x] 1.2 在审核创建用户时写入 `tb_user.email`（来源于报名邮箱）。
- [x] 1.3 按 `SHA-256 -> BCrypt` 管线处理初始密码后写入 `tb_user.password`。
- [x] 1.4 保持“已存在用户”分支非破坏行为（不轮转密码、不重新生成凭据）。

## 2. 审核通过邮件通知

- [x] 2.1 在现有通知路径新增硬编码 HTML 模板（与当前验证码模板风格一致）。
- [x] 2.2 审核创建新用户时异步发送凭据邮件，邮件包含明文初始密码与首登改密提示。
- [x] 2.3 保证邮件发送失败可观测（日志可追踪），且不回滚审核通过主流程。

## 3. 测试与回归验证

- [x] 3.1 扩展审核通过新用户分支测试，验证邮箱落库与 `SHA-256 -> BCrypt` 存储行为。
- [x] 3.2 补充/调整测试，验证已存在用户分支不改密且不发送初始凭据邮件。
- [x] 3.3 补充/调整通知测试，验证异步发送调用与邮件内容（含明文初始密码）。
