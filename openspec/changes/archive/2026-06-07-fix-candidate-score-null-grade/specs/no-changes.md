# No Spec Changes

This bug fix is purely at the implementation level (SQL query logic). No specification-level requirements are being added, modified, or removed.

The existing `assessment-judgement` spec already covers the expected behavior of candidate scoreboard filtering. The fix only corrects the SQL null handling to align with that spec.
