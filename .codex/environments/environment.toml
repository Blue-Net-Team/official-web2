# THIS IS AUTOGENERATED. DO NOT EDIT MANUALLY
version = 1
name = "develop"

[setup]
script = '''
Set-Location -LiteralPath "$env:CODEX_WORKTREE_PATH/src/frontend"
pnpm install
if ($LASTEXITCODE -ne 0) {
  exit $LASTEXITCODE
}
$backendEnv = Join-Path $env:CODEX_WORKTREE_PATH "src/backend/.env"
if (-not (Test-Path -LiteralPath $backendEnv)) {
  Copy-Item -LiteralPath "$env:CODEX_SOURCE_TREE_PATH/src/backend/.env" -Destination $backendEnv -ErrorAction Stop
}
'''
