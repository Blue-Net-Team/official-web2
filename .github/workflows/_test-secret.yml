# 临时诊断：检查可复用工作流能否访问 repo secret
name: Test Secret
on:
  workflow_call:
    inputs:
      label:
        required: true
        type: string
jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - run: echo "RESULT[${{ inputs.label }}]=${{ secrets.DATABASE_HOST_DEV }}"
